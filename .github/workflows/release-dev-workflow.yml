# SPDX-FileCopyrightText: 2025 The Open Source Project Template Authors
#
# SPDX-License-Identifier: CC0-1.0
name: Release Dev Workflow
on:
  workflow_dispatch:
permissions:
  contents: read
jobs:
  dev-release:
    uses: diggsweden/reusable-ci/.github/workflows/release-dev-orchestrator.yml@617cb32268cb6aefdfa4117b4159d37e3b47d7b3 # v2.8.3
    permissions:
      contents: read
      packages: write
    secrets: inherit
    with:
      reusable-ci-ref: d31c989acd139c3e8754e116496c826af8dbbca0
      # This template is documentation/reference - not a buildable project
      # Projects using this template should set their actual type:
      # maven, npm, gradle, python
      project-type: maven
      container-file: Containerfile
      working-directory: .
      # For NPM projects, enable NPM publishing:
      # publish-npm: true
      # node-version: "22"
